import { NextPage } from "next"
import GuidelinePage from "@/p-components/GuidelinePage"

interface Props {}

const Guidelines: NextPage<Props> = ({}) => {
  return (
    <div>
      <GuidelinePage />
    </div>
  )
}

export default Guidelines
